DATABASE_URL=postgres://postgres:postgres@localhost:5433/dbc_test
# Set by Vercel Cron in production; used to authenticate /api/cron/expire-drafts.
# Leave empty locally — the cron route skips the check outside production.
CRON_SECRET=
